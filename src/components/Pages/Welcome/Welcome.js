import styled from "styled-components";

export const Container = styled.div`  
  padding-top: var(--nav-height);
  padding-inline: 3rem;

  background: linear-gradient(to right, rgba(193,27,27, 0.4), rgba(2,2,2, 0.5 ));
`
